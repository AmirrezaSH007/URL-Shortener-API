# Copy this to .env and adjust as needed
PORT=3000
# For local dev (non-Docker), start a Postgres and set DATABASE_URL accordingly:
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/shortener
DATABASE_URL=postgres://postgres:postgres@db:5432/shortener

# For tests, a separate database is recommended; docker-compose provides it as 'shortener_test'
TEST_DATABASE_URL=postgres://postgres:postgres@db:5432/shortener_test
